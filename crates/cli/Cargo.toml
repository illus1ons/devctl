[package]
name = "devctl"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "devctl"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
colored = "3.1.1"
doctor = { path = "../doctor" }
