#:schema https://raw.githubusercontent.com/illus1ons/devctl/main/schemas/dev.schema.json

[project]
name = "devctl"

[doctor]
tools = ["git", "cargo"]

[services.db]
type = "docker"
port = 5432

[services.redis]
type = "docker"
port = 6379
